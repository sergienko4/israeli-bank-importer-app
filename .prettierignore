# Build output and generated files
dist/
.expo/
node_modules/
coverage/
android/
ios/
*.log
# Markdown is validated by markdownlint (.markdownlint.jsonc), not Prettier
*.md
# release-please owns this file's serialization (release-type: expo). It parses
# and re-stringifies app.json on every release, which expands short arrays that
# Prettier collapses. Checking it makes every release PR fail format:check.
app.json
# The vendored API contract is a byte-identical copy of the importer's
# src/Contract, and `npm run contract:check` compares it against that repo.
# Reformatting it here would make every check report drift that is really just
# a difference of opinion about line breaks.
src/api/generated/
